# def add_two_numbers(number1, number2):
#     print(number1 + number2)
# add_two_numbers(2,3)

def square_three_numbers(a, b, c):  # Creating a function # a,b,c -> parameters / placeholders

    return a**2, b**2, c**2 # you should always use return statement in a function rather than a print statement.
    # You can return only once from a function taking into consideration the indentation

print("kaustubh")

print(square_three_numbers(2, 3, 4))  # Calling the function / Executing the function # 2,3,4 -> arguments


